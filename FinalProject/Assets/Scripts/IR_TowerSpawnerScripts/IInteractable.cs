
interface IInteractable
{
    // Interactable objects will need to implement this interface.
    // invoke the function from a class that implements IInteractable to
    // achieve whichever interact functionality we need.
    void PerformInteractAction();
}
